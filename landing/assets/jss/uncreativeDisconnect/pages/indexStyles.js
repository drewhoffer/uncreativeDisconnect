import {
    defaultFont,
    primaryColor,
    secondaryColor,
    tertiaryColor
} from "../../uncreativeDisconnect.js";

const indexStyles = {
    red: {
        color: "red"
    }
};

export default indexStyles;